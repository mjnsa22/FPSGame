using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class minimap : MonoBehaviour
{
    //public GameObject marker;
    //public GameObject[] targets;
    //private GameObject[] markers;
    //// Start is called before the first frame update
    //void Start()
    //{
    //    markers = new GameObject[targets.Length];
    //    for (int i = 0; i < targets.Length; i++)
    //    {
    //        var mark = Instantiate(marker);
    //        markers.SetValue(mark, i);
    //    }
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    for (int i = 0; i < markers.Length; i++)
    //    {
    //        GameObject marker = (GameObject)markers.GetValue(i);
    //        GameObject target = (GameObject)targets.GetValue(i);
    //        marker.transform.position.x = target.transform.position.x;
    //        marker.transform.position.y = target.transform.position.z;
    //    }
    //}
}
